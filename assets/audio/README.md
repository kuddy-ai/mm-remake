# Audio Assets

运行时音频素材目录。

## 目录结构

```text
audio/
├── music/     # 背景音乐
├── sfx/       # 音效
└── voice/     # 语音
```

## 规范

- 使用 `*.mp3` 或 `*.ogg` 格式
- 命名使用英文小写、数字序号、下划线
- 例如：`opening_theme.mp3`, `battle_victory.ogg`