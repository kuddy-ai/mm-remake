# External Assets Storage

此目录用于存放原始设计文件和大文件，**不会提交到 Git**。

## 存放内容

- PSD、AI、KRA、CLIP 等源文件
- TIFF、超大 PNG 原始设计图
- 高清视频素材
- 其他不适合提交的大型素材

## 压缩后的参考图

压缩后的参考图放在 `docs/concept-art/wasteland_hunter_curated_pixel_designs/`。

## 注意

此目录已加入 `.gitignore`，请勿将此目录提交到 Git。